<h1>Add a New Product</h1>

<% form_for(@product) do |@f| %>
  <%= @f.error_messages %>
  <%= render :partial => 'form' %>
  <p>
    <%= @f.submit "Create", :class => 'submit' %>
  </p>
<% end %>

<%= link_to 'Back', products_path %>
