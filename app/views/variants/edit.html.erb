<% content_for :subnavigation do %>
<%= render :partial => 'products/subnavigation' %>
<% end -%>

<h1>Edit a New Variant</h1>

<% form_for([@product, @variant]) do |@f| %>
  <%= @f.error_messages %>
  <%= render :partial => 'form' %>
  <p>
    <%= @f.submit "Update", :class => 'submit' %>
  </p>
<% end %>

<%= link_to 'Back', product_variants_path %>
